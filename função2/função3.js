let dolar = parseFloat( prompt('Digite quantos dólares você tem'));
let media 

function converter(pdolar) {
    let total = (pdolar)/4.80;
    return total
}

media = converter(dolar);
console.log (`${media.toFixed(2)}`);